En cmd de visual studio o code o mismo de Windows hay que hacer cd y ir al directorio de la app, si tenemos instalado npm, next y hasta tailwind, solo hace falta usar este comando:
npm run dev

luego en el navegador que mas se prefiera accedemos a la url del localhost:

http://localhost:3000/support
